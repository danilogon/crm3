// Função simulada para compatibilidade com o código existente
export const seedDatabase = async () => {
  console.log('🌱 Simulando seed do banco de dados...');
  console.log('✅ Seed simulado concluído com sucesso!');
  return true;
};